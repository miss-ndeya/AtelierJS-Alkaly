// Créez une variable appelée   numberOfSeasons  et assignez-lui la valeur 6 entre
// les lignes créées pour vous dans l'éditeur JavaScript (l'onglet appelé "JS").
//Pensez à utiliser le mot clé let, et n'oubliez pas le point-virgule en fin de ligne !
// Ensuite, créez une variable appelée  numberOfEpisodes  et assignez-lui la valeur 12.

// ========================

let numberOfSeasons = 6;
let numberOfEpisodes = 12;

// ========================

// Dans l'éditeur JavaScript, créez deux variables,  episodeTime  et  commercialTime ("temps de 
// l'épisode" et "temps des pubs"), avec des valeurs appropriées (45 et 5, par exemple).

// ========================

let episodeTime = 45;
let commercialTime = 5;

// ========================

// À l'aide des deux variables de temps que vous venez de créer, ainsi que du nombre 
// d'épisodes et du nombre de saisons (variables préexistantes), créez une variable 
// nommée  totalShowTime  qui contient le temps de visionnage total de cette série.

// ========================

let somme = episodeTime + commercialTime;
let multiplication = numberOfSeasons * numberOfEpisodes;
let  totalShowTime = somme * multiplication;
console.log(totalShowTime);

// ========================

// Découvrez les constantes
// Dans l'espace alloué, créez les trois constantes suivantes :
// hoursPerDay: heures par jour
// minutesPerHour: minutes par heure
// secondsPerMinute: secondes par minute
// Initialisez-les avec les valeurs correctes.

// ========================

let hoursPerDay = 24;
let minutesPerHour = 60;
let secondsPerMinute = 60;

// ========================

// Créez une variable nommée  episodeTitle  qui contient une chaîne de caractères 
// correspondant au titre de votre épisode (employez votre créativité !).

// ========================

let episodeTitle = "Type de donnee";

// ========================

// Créez une variable appelée  episodeDuration  qui contient un nombre correspondant 
// à la durée de votre épisode en minutes.

// ========================

let episodeDuration = 14;

// ========================

// Enfin, créez une variable booléenne nommée   hasBeenWatched  qui indique
//  si l'utilisateur a déjà regardé l'épisode ou non.

// ========================

let hasBeenWatched = true;

// ========================



