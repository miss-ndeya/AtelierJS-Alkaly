// En utilisant la boucle while(), afficher dans la console 
// tous les codes postaux possibles pour le département 77.
let codespostaux = 77000;
while (codespostaux <= 77900){
    console.log({codespostaux});
    codespostaux++
}